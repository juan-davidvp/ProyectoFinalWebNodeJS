<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acceso Davivienda</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="icon" href="imagenes/favicon.ico" type="image/x-icon">
    <style>
        /* Estilos generales del cuerpo de la página - FULLPAGE */
        body {
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
            height: 100vh; /* Altura completa de la ventana */
            overflow: hidden; /* Evitar scroll */
        }

        /* Contenedor principal - FULLPAGE con espaciado entre columnas */
        .auth-wrapper {
            display: flex;
            height: 100vh; /* Altura completa */
            width: 100vw; /* Ancho completo */
            gap: 2rem; /* ESPACIADO entre las dos columnas */
            padding: 2rem; /* Padding general del contenedor */
            box-sizing: border-box;
        }

        /* Columna izquierda para la imagen - Ajustada al diseño */
        .auth-image-column {
            
            background-color: #e31e24;
            color: white;
            padding: 3rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            border-radius: 25px; /* Bordes más redondeados */
            background-image: linear-gradient(rgba(247, 202, 203, 0.192), rgba(227, 30, 36, 0.7)), url('imagenes/oficinas.jpg');
            background-size: cover;
            background-position: center;
            flex: 1; /* Toma el espacio disponible */
            min-height: 0; /* Para que funcione correctamente el flex */
        }

        .auth-image-column h3 {
            margin-bottom: 0.5rem; 
            font-weight: 200; 
            font-size: 2rem;
            text-shadow: #343a40 1px 1px 2px; /* Sombra para el texto */
        }

        .auth-image-column p {
            font-size: 3rem;
            font-weight: 600;
            text-shadow: #343a40bd 0px 5px 10px; 
        }

        /* Columna derecha para los formularios - Ajustada al diseño */
        .auth-form-column {
            background-color: #fff;
            padding: 3rem;
            border-radius: 25px; /* Bordes más redondeados */
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            flex: 1; /* Toma el espacio disponible */
            display: flex;
            flex-direction: column;
            justify-content: center;
            min-height: 0;
        }

        /* Logo de Davivienda */
        .davivienda-logo-auth {
            display: block;
            margin: 0 auto 2rem auto;
            height: 94px;
        }

        /* Pestañas más redondeadas */
        .nav-tabs {
            border-bottom: none;
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .nav-tabs .nav-link {
            flex-grow: 1;
            text-align: center;
            background-color: #f8f9fa;
            color: #6c757d;
            border: 1px solid #dee2e6;
            border-radius: 25px; /* MÁS REDONDEADOS */
            margin: 0 0.25rem;
            font-weight: 500;
            padding: 0.75rem 1.5rem;
            transition: all 0.3s ease;
        }

        .nav-tabs .nav-link.active {
            background-color: #e31e24; /* Color marrón como en la imagen */
            color: white;
            border-color: #e31e24;
            font-weight: bold;
        }

        .nav-tabs .nav-link:not(.active):hover {
            background-color: #e9ecef;
        }

        /* Títulos y textos */
        .auth-form-column h2 {
            color: #343a40;
            margin-bottom: 0.4rem;
            text-align: center;
            font-size: 2rem;
            font-weight: bold;
        }

        .auth-form-column .form-text-description {
            text-align: center;
            color: #6c757d;
            font-size: 1rem;
            margin-bottom: 2rem;
        }

        /* Labels de formulario */
        .form-label {
            font-weight: 600;
            color: #495057;
            font-size: 0.95rem;
            margin-bottom: 0.5rem;
        }

        /* Campos de entrada MÁS REDONDEADOS */
        .form-control {
            border-radius: 25px; /* MÁS REDONDEADOS como en la imagen */
            border: 2px solid #e9ecef;
            padding: 0.875rem 1.25rem;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            background-color: #f8f9fa;
        }

        .form-control:focus {
            border-color: #bd181d; /* Color marrón del diseño */
            box-shadow: 0 0 0 0.2rem rgba(139, 69, 19, 0.25);
            background-color: #fff;
        }

        .form-control::placeholder {
            color: #adb5bd;
        }

        /* Checkbox */
        .form-check-label {
            font-size: 0.9rem;
            color: #495057;
        }

        .form-check-input:checked {
            background-color: #e31e24;
            border-color: #e31e24;
        }

        /* Botón principal MÁS REDONDEADO */
        .btn-auth-action {
            background-color: #e31e24; /* Color marrón como en la imagen */
            border-color: #e31e24;
            color: white;
            font-weight: bold;
            padding: 0.875rem 0;
            font-size: 1rem;
            border-radius: 25px; /* MÁS REDONDEADO */
            transition: all 0.3s ease;
            margin-top: 1rem;
        }

        .btn-auth-action:hover {
            background-color: #a70d12;
            border-color: #a70d12;
            color: white;
            transform: translateY(-1px);
        }

        /* Mensajes de éxito/error */
        .auth-message {
            padding: 0.75rem 1.25rem;
            margin-top: 1rem;
            border-radius: 15px;
            text-align: center;
            font-size: 0.9rem;
        }

        .auth-message.success {
            color: #0f5132;
            background-color: #d1e7dd;
            border: 1px solid #badbcc;
        }

        .auth-message.error {
            color: #842029;
            background-color: #f8d7da;
            border: 1px solid #f5c2c7;
        }

        /* Transición para tab-panes */
        .tab-content > .tab-pane {
            display: none;
        }

        .tab-content > .active {
            display: block;
        }

        .toast-container {
            z-index: 2; /* Asegurar que el toast esté por encima de otros elementos */
            position: fixed;
            text-align: center;   
        }

        .toast-container h4{
            text-align: center;   
        }


        /* Espaciado entre campos */
        .mb-3 {
            margin-bottom: 1.5rem !important;
        }

        /* Media queries para responsive */
        @media (max-width: 991.98px) {
            .auth-wrapper {
                flex-direction: column;
                gap: 1.5rem;
                padding: 1.5rem;
                overflow-y: auto;
                height: auto;
                min-height: 100vh;
            }

            body {
                overflow: auto;
            }

            .auth-image-column {
                min-height: 300px;
                flex: none;
            }

            .auth-form-column {
                flex: none;
                padding: 2rem;
            }
        }

        @media (max-width: 767.98px) {
            .auth-wrapper {
                gap: 1rem;
                padding: 1rem;
            }

            .auth-image-column {
                min-height: 200px;
                padding: 2rem;
            }

            .auth-image-column h3 {
                font-size: 1.4rem;
            }

            .auth-image-column p {
                font-size: 1rem;
            }

            .auth-form-column {
                padding: 1.5rem;
            }

            .nav-tabs .nav-link {
                padding: 0.6rem 1rem;
                font-size: 0.9rem;
            }


            .davivienda-logo-auth {
            height: 50px;
        }
        }
    </style>
</head>
<body>
    <div class="auth-wrapper">
        <!-- Columna izquierda con imagen y texto -->
        <div class="auth-image-column">
            <div class=" px-5">
                <h3>En estos momentos su dinero puede estar en el lugar equivocado</h3>
                <p>¡Tráigalo a Davivienda!</p>
            </div>
        </div>

        <!-- Columna derecha con formularios -->
        <div class="auth-form-column">
            <img src="imagenes/logos/Davivienda_Logo.png" alt="Logo Davivienda" class="davivienda-logo-auth">

            <!-- Pestañas de navegación -->
            <ul class="nav nav-tabs" id="authTab" role="tablist">
                <!-- Tab Login -->
                <li class="nav-item" role="presentation">
                    <button class="nav-link {{#ifeq activeTab 'login'}}active{{else}}{{#unless activeTab}}active{{/unless}}{{/ifeq}}" id="login-tab" data-bs-toggle="tab" data-bs-target="#login-tab-pane" type="button" role="tab" aria-controls="login-tab-pane" aria-selected="true">Ingreso</button>
                </li>
                <!-- Tab Register -->
                <li class="nav-item" role="presentation">
                    <button class="nav-link {{#ifeq activeTab 'register'}}active{{/ifeq}}" id="register-tab" data-bs-toggle="tab" data-bs-target="#register-tab-pane" type="button" role="tab" aria-controls="register-tab-pane" aria-selected="{{#ifeq activeTab 'register'}}true{{else}}false{{/ifeq}}" id="register-tab" data-bs-toggle="tab" data-bs-target="#register-tab-pane" type="button" role="tab" aria-controls="register-tab-pane" aria-selected="false">Registro</button>
                </li>
            </ul>

            <!-- Contenido de las pestañas -->
            <div class="tab-content px-5" id="authTabContent">
                <!-- Formulario de Login -->
                <div class="tab-pane fade {{#ifeq activeTab 'login'}}show active{{else}}{{#unless activeTab}}show active{{/unless}}{{/ifeq}}" id="login-tab-pane" role="tabpanel" aria-labelledby="login-tab" tabindex="0">
                    <h2>Bienvenido de nuevo!</h2>
                    <p class="form-text-description">Nos alegra que disfrutes de Davivienda</p>
                    
                    <form action="/auth/login" method="post">
                        <div class="mb-3">
                            <label for="loginEmail" class="form-label">Email</label>
                            <input type="email" class="form-control" id="loginEmail" name="email" placeholder="Ingresa aquí tu correo electrónico" required>
                        </div>
                        
                        <div class="mb-3">
                            <label for="loginPassword" class="form-label">Contraseña</label>
                            <input type="password" class="form-control" id="loginPassword" name="password" placeholder="Ingresa aquí tu contraseña" required>
                        </div>
                        
                        <button type="submit" class="btn btn-auth-action w-100">Ingresar</button>

                        {{#if message}}
                            <p class="auth-message success">{{message}}</p>
                        {{/if}}
                        {{#if error_login}}
                            <p class="auth-message error">{{error_login}}</p>
                        {{/if}}
                    </form>
                </div>

                <!-- Formulario de Registro -->
                <div class="tab-pane fade {{#ifeq activeTab 'register'}}show active{{/ifeq}}" id="register-tab-pane" role="tabpanel" aria-labelledby="register-tab" tabindex="0">
                    <h2>¡Sé parte del mejor Banco de Colombia!</h2>
                    <p class="form-text-description">Disfruta de multiples beneficios que tiene Davivienda para ti</p>
                    
                    <form action="/auth/register" method="post">
                        <div class="mb-3">
                            <label for="registerName" class="form-label">Nombre</label>
                            <input type="text" class="form-control" id="registerName" name="name" placeholder="Ingresa aquí tu nombre" required>
                        </div>
                        
                        <div class="mb-3">
                            <label for="registerEmail" class="form-label">Email</label>
                            <input type="email" class="form-control" id="registerEmail" name="email" placeholder="Ingresa aquí tu correo electrónico" required>
                        </div>
                        
                        <div class="mb-3">
                            <label for="registerPassword" class="form-label">Contraseña</label>
                            <input type="password" class="form-control" id="registerPassword" name="password" placeholder="Ingresa aquí tu contraseña" required>
                        </div>

                        <div class="mb-3">
                            <label for="registerPassword" class="form-label">Confirma la Contraseña</label>
                            <input type="password" class="form-control" id="passwordConfirm" name="passwordResend" placeholder="Ingresa aquí tu contraseña" required>
                        </div>
                        
                        <button type="submit" class="btn btn-auth-action w-100">Register</button>

                        {{#if message}}
                            <p class="auth-message success">{{message}}</p>
                        {{/if}}
                        {{#if error_login}}
                            <p class="auth-message error">{{error_login}}</p>
                        {{/if}}
                    </form>
                </div>

                <div class="toast-container position-fixed bottom-0 end-0 p-5">
                    <div id="registrationToast" class="toast text-center text-bg-success border-0" role="alert" aria-live="assertive" aria-atomic="true">
                        <div class="toast-header d-flex">
                            <strong class="me-auto">Registro Exitoso</strong>
                            <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
                        </div>
                        <div class="toast-body " id="toastBodyMessage">
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script>
        // Inicialización de las pestañas de Bootstrap
        document.addEventListener('DOMContentLoaded', function () {
            var triggerTabList = [].slice.call(document.querySelectorAll('#authTab button'));
            triggerTabList.forEach(function (triggerEl) {
                var tabTrigger = new bootstrap.Tab(triggerEl);

                triggerEl.addEventListener('click', function (event) {
                    event.preventDefault();
                    tabTrigger.show();
                });
            });
        });
    </script>
    <script>

        document.addEventListener('DOMContentLoaded', function () {
            // Variables pasadas desde el contexto de Handlebars por el servidor
            const showToastFromServer = {{#if showToast}}true{{else}}false{{/if}};
            const registeredUserFromServer = {{#if registeredUser}}{{{json registeredUser}}}{{else}}null{{/if}};
            const registeredMailFromServer = {{#if registeredMail}}{{{json registeredMail}}}{{else}}null{{/if}};

            // Variable para la duración del Toast en milisegundos
            // Por defecto, Bootstrap usa 5000 (5 segundos). Puedes cambiar este valor.
            const toastDurationInMilliseconds = 3000; // Ejemplo: 7 segundos

            const toastElement = document.getElementById('registrationToast');
            const toastBodyMessageElement = document.getElementById('toastBodyMessage');
            
            if (showToastFromServer && registeredUserFromServer && registeredMailFromServer && toastElement && toastBodyMessageElement) {
                // Construir el mensaje del toast
                toastBodyMessageElement.textContent = `El usuario: ${registeredUserFromServer}, con correo: ${registeredMailFromServer} se ha registrado exitosamente!`;
                
                // Inicializar y mostrar el toast con la duración configurada
                const toast = new bootstrap.Toast(toastElement, {
                    delay: toastDurationInMilliseconds, // Establece la duración
                    autohide: true // Asegúrate de que autohide esté en true (es el valor por defecto)
                });
                toast.show();

                // Es importante que la pestaña de registro esté activa si se muestra el toast.
                const registerTabButton = document.getElementById('register-tab');
                if (registerTabButton && !registerTabButton.classList.contains('active')) {
                    const tab = new bootstrap.Tab(registerTabButton);
                    tab.show();
                }

                // Añadir un event listener para cuando el toast se oculte
                toastElement.addEventListener('hidden.bs.toast', function () {
                    // Activar la pestaña de Login
                    const loginTabButton = document.getElementById('login-tab');
                    if (loginTabButton) {
                        const tab = new bootstrap.Tab(loginTabButton);
                        tab.show();
                    }
                });
            }
        });
    </script>
</body>
</html>